server:
  port: 8083
  servlet-path: /*

spring:
  datasource:
    username: sa
    url: jdbc:h2:mem:testdb

keycloak:
  server:
    contextPath: /auth
    adminUser:
      username: admin
      password: pass
    realmFiles: test-realm.json
